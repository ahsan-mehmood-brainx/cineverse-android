package com.example.cineverse.data.remote.api

/**
 * Retrofit endpoints for The Movie DB API.
 * Intentionally empty — endpoints are added alongside their DTOs and repository calls.
 */
interface TMDBApi
